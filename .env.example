# Environment example for forms/contact.php
# For SendGrid (recommended)
SENDGRID_API_KEY=

# For SMTP via PHPMailer (alternative)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
SMTP_SECURE=tls  # or ssl

# From address used in outgoing emails (change to your domain)
FROM_ADDRESS=noreply@yourdomain.com

# Optional override for recipient (if you want to test)
RECIPIENT_EMAIL=ezeonyekasinachi@gmail.com
